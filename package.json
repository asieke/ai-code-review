{
  "name": "ai-code-review",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "push": "git add . && git commit -am \"$(date '+%Y-%m-%d %H:%M:%S')\" && git push origin main && git tag -d v1.1 && git push origin --delete v1.1 && git tag -a v1.1 -m 'first release' && git push origin v1.1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@octokit/rest": "^20.0.1",
    "node-fetch": "^3.3.2"
  }
}
